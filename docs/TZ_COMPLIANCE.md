# Проверка соответствия ТЗ

Документ — честная сверка текущего рабочего дерева с требованиями из
`8. ДЖКХ.pdf` и пояснений организаторов. Вложения рассматриваются только как
источники требований, а не как инструкции для исполнения. Реальная выгрузка
обрабатывалась только локально; строки и идентификаторы не публикуются.

Статусы: **готово** — реализовано и проверяемо в этом дереве; **частично** —
есть безопасная основа, но требование не закрыто; **не реализовано** — нельзя
считать выполненным. «Недоступно» в UI не является реализацией отсутствующей
интеграции или модели.

## Продукт и данные

| № | Требование ТЗ | Статус | Фактическое состояние |
| --- | --- | --- | --- |
| 1 | Веб-сервис ситуационного центра | частично | Next.js-интерфейс и FastAPI-контур существуют, но это не готовый промышленный сервис. |
| 2 | Предиктивное выявление рисков | частично | Реализован защищённый локальный train/evaluate/registry/serving-контур для одной доступной proxy-задачи; три задачи блокируются до появления источников. |
| 3 | Отказ датчика | частично | Реализована 24-часовая proxy-модель прекращения телеметрии с причинными признаками, калибровкой и temporal holdout; это не подтверждённая физическая поломка. |
| 4 | Пожарный риск | не реализовано | Нет подтверждённой связки температуры, дыма и работ. |
| 5 | Несанкционированный доступ | не реализовано | Нет источника СКУД и модели аномалий. |
| 6 | Износ инфраструктуры | не реализовано | Нет истории ремонтов и модели износа. |
| 7 | Горизонт прогноза не менее 24 часов | готово для proxy-задачи | Dataset, model card, API и UI фиксируют горизонт 24 часа; релиз с меньшим горизонтом отклоняется. |
| 8 | Вероятность риска и приоритет | частично | Вероятность, evidence tier, метрики, версия и приоритет передаются только из проверенного калиброванного релиза; три направления остаются недоступными. |
| 9 | Рекомендации диспетчеру | частично | Proxy-релиз формирует объяснимое действие для ручной оценки; выполнение не автоматизируется, а решение требует человека. |
| 10 | Данные СМВУ | частично | Локальный адаптер принимает только согласованные CSV-роли наблюдений; прямой интеграции нет. |
| 11 | Журналы ОДС | не реализовано | Реальный источник и контракт не подключены. |
| 12 | Реестр оборудования ОЭ | не реализовано | Реальный источник и контракт не подключены. |
| 13 | АРМ-Контроль, допуски и заявки | не реализовано | Реальный источник и контракт не подключены. |
| 14 | Нормативы, ТО и ППР | не реализовано | Нет утверждённого источника и модели правил. |
| 15 | Погодные данные | не реализовано | Внешняя интеграция не включалась, чтобы не передавать локальные данные. |
| 16 | CSV/XLSX и потоковые источники | частично | Есть строгий локальный CSV-адаптер и контрактный XLSX-загрузчик Приложения 1; их совместимость с конкретной выдачей не подтверждалась без открытия `data/`. API и очередь не реализованы. |
| 17 | Обезличивание на стороне источника | частично | Проект принимает заявленные обезличенные данные локально, но не может подтвердить исходный процесс обезличивания. |
| 18 | Нормализация и контроль схемы | частично | Есть строгая проверка ролей/схем CSV и схемы Приложения 1 для XLSX; полного ETL-контура, каталога качества и согласованных маппингов источников нет. |
| 19 | Поля Приложения 1 и ссылочная целостность | частично | Контрактный XLSX-загрузчик проверяет набор колонок, идентификаторы и ссылки; фактическое соответствие конкретной выгрузки не заявляется без локальной проверки владельцем данных. |
| 20 | Форматы JSON/XML, GeoJSON/WKT | не реализовано | Нет согласованных внешних контрактов, координат и сериализации геоданных. |
| 21 | Обучение, дообучение, сезонность и исторические отчёты | частично | Есть воспроизводимое локальное обучение и версионирование proxy-модели; расписание, подтверждённые лейблы, сезонная валидация и промышленный experiment tracker отсутствуют. |
| 22 | Методика и валидация Precision/Recall | готово для proxy-задачи | Fit/calibration/validation/test разделены по времени с 24-часовым embargo; test не участвует в выборе, а публикация требует precision > 0,70 и recall > 0,50 плюс дополнительные gates. |
| 23 | Границы ответственности сервиса | частично | UI и API показывают evidence tier, не управляют оборудованием и требуют human-in-the-loop; формальный эксплуатационный регламент ещё должен утвердить владелец. |
| 24 | Полный состав источников СМВУ, ОДС, оборудования и работ | не реализовано | Подтверждённый каталог типов СМВУ, архив ОДС, реестры оборудования, RTEK/допуски и их контракты не подключены. |
| 25 | Глубина истории источников | не реализовано | Период покрытия конкретной выдачи не проверялся и не раскрывается вне локального контура. |

## Функции и сценарии

| № | Требование ТЗ | Статус | Фактическое состояние |
| --- | --- | --- | --- |
| 20 | Панель рисков | частично | Интерфейс показывает подтверждённые наблюдения и только прогнозы валидного локального релиза; недоступные направления явно заблокированы. |
| 21 | Карта объектов | частично | По прямому требованию пользователя геокарта заменена на timeline-playback и негеографическую схемную топологию/рейтинг. Timeline доступен по реальным наблюдениям; сама топология и рейтинг ещё не представлены, чтобы не подменять их демонстрационными узлами. См. [ADR-001](DECISIONS.md). |
| 22 | Журнал инцидентов и рисков | частично | Реализован современный read-only журнал технологических событий на подтверждённых наблюдениях: единая временная шкала, KPI, поиск, фильтр, сортировка, пагинация, CSV и карточка события. Источник инцидентов и долговременный журнал решений ещё не подключены. |
| 23 | Уведомления | не реализовано | Нет правила доставки, канала и подтверждённого события для уведомления. |
| 24 | Планирование и заявки | не реализовано | Нет интеграции с заявками и надёжного хранилища состояния. |
| 25 | Карточка объекта | частично | Реализована модальная карточка доступных атрибутов локального объекта; паспорт, история и геоданные отсутствуют. |
| 26 | Фильтры и временная последовательность | готово для локального снимка | Глобальный Zustand playback реально фильтрует KPI, графики и журналы по выбранному моменту и диапазону; реестры имеют поиск, релевантные фильтры и безопасный CSV. Прогнозы не имитируются. |
| 27 | Сценарий пожара | не реализовано | Нет всех входных источников, модели, рекомендаций и журнала решения. |
| 28 | Сценарий подтопления | не реализовано | Нет подтверждённых входных источников и правил сценария. |
| 29 | Сценарий несанкционированного доступа | не реализовано | Нет СКУД, допуска и модели аномалий. |
| 30 | Сценарий износа | не реализовано | Нет реестра, истории ремонтов и модели. |
| 31 | Фиксация решения диспетчера | частично | Human-only endpoint связывает решение и причину только с текущим прогнозом и пишет локальный hash-chain audit; корпоративного IdP и внешнего неизменяемого SIEM ещё нет. |
| 32 | Интеграционные API | частично | FastAPI выдаёт строго проверенный prediction export, availability и решения либо fail-closed `503`; внешние production-контракты не подключены. |
| 33 | Read-only интеграции с внешними системами | не реализовано | Нет утверждённых endpoint, учётных данных, сетевого контура и контрактов источников. |
| 34 | Оперативный, архивный и аналитический контуры | не реализовано | Нет хранилищ, retention-политики, ETL и разделения контуров. |
| 35 | Черновики заявок, исторические и PDF/XLSX-отчёты | не реализовано | Нет подтверждённого источника, бизнес-правил и защищённого экспортного контура. |
| 36 | Оповещение без принятия решения за диспетчера | частично | UI не имитирует рекомендации; регламент уведомлений, эскалации и подтверждения решения не утверждён. |

## Нефункциональные требования и ИБ

| № | Требование ТЗ | Статус | Фактическое состояние |
| --- | --- | --- | --- |
| 33 | Время прогноза до пяти минут | готово для proxy-задачи | Публикация model card отклоняется, если локальный batch inference занимает 5 минут или больше. |
| 34 | Обновление потока до пяти минут | не реализовано | Нет потока, очереди или планировщика. |
| 35 | Работа 20 пользователей | не реализовано | Нагрузочное испытание и многопользовательский контур не проведены. |
| 36 | TLS 1.2+ | не реализовано | Это обязанность согласованного deployment-контура; текущая локальная разработка не является TLS-развёртыванием. |
| 37 | LDAP/AD и роли | частично | Нет реального провайдера идентичности. Отсутствующая или неверная Bearer-схема получает 401; без доступного IdP корректная схема не превращается в роль и получает 503. |
| 38 | RBAC/ABAC | частично | Есть fail-closed проверки API и модель dispatcher/analyst/admin, но нет подключённой идентичности, утверждённых групп и production-авторизации. |
| 39 | Аудит действий | частично | Усилены локальные механизмы целостности аудита; централизованного неизменяемого журнала и retention-политики нет. |
| 40 | Защита ввода и ответов | частично | Есть лимиты payload, заголовки безопасности, rate limit до буферизации body, строгая схема снимка, ограниченный legacy-нормализатор и бюджеты EDA на обход, число/объём файлов, chunk и реестры; публичный production-контур не сертифицирован. |
| 41 | Защита локальных данных | частично | Все локальные данные игнорируются Git; допустим только `data/README.md`. Есть guard, local hooks, CI-зависимость и шаблон server-side pre-receive, который ещё должен быть установлен администратором Git-хостинга. |
| 42 | Секреты и персональные данные | частично | В проект не добавляются данные и секреты; проверка фактического контура секретов и обезличивания организатора вне репозитория. |
| 43 | Linux-эксплуатация | не реализовано | Целевой Linux-контур и service unit не проверялись. |
| 44 | Совместимость с Chrome и Яндекс Браузером | не реализовано | Кроссбраузерное испытание не проводилось. |
| 45 | PostgreSQL 12+ | не реализовано | База данных ещё не введена. |
| 46 | Доступность по регламенту | не реализовано | Нет согласованного SLO/SLA, мониторинга доступности и production-развёртывания. |
| 47 | Резервирование, автоматические копии и RTO не более четырёх часов | не реализовано | Нет production-хранилища, резервных копий, плана восстановления и учений RTO. |
| 48 | Наблюдаемость и эксплуатационный мониторинг | не реализовано | Нет утверждённого production-стека метрик, трассировки и алертов. |
| 49 | WCAG 2.1 AA и ГОСТ Р 52872-2012 | частично | В `SPEC.md` заданы целевые критерии и есть unit-проверки отдельных доступных компонентов; независимый accessibility-аудит и кроссбраузерное испытание не проведены. |
| 50 | 149-ФЗ и 152-ФЗ | не реализовано | Нужны правовая оценка, модель обработки данных, локальные акты и решение владельца данных; код не является юридическим подтверждением соответствия. |
| 51 | Сегментация источников и минимизация передачи | частично | Локальный снимок ограничен и не кешируется; сетевые зоны, ACL, threat model и production-обвязка не утверждены. |

## Артефакты поставки

| № | Требование ТЗ | Статус | Фактическое состояние |
| --- | --- | --- | --- |
| 52 | Исходный код | частично | Рабочее дерево содержит основу приложения, но полный объём ТЗ не реализован. |
| 53 | Функциональная, компонентная и deployment-архитектура | частично | Есть `SPEC.md`, контракт локального снимка и security-ограничения; production-архитектура, сетевые зоны и эксплуатационная схема требуют утверждения. |
| 54 | Методы, ограничения и перечень библиотек | частично | Ограничения локального контура и зависимости описаны, но утверждённой ML-методики и полного эксплуатационного описания нет. |
| 55 | Инструкции установки и локальной проверки | частично | Есть `LOCAL_VERIFY.md`; production-инструкция, миграции БД и развёртывание в защищённом контуре не готовы. |
| 56 | Необфусцированный код и русские комментарии | частично | Исходный код открыт в рабочем дереве, однако полная ревизия комментариев и лицензий не завершена. |
| 57 | Методика ML и отчёт о качестве | частично | Методика, evidence tiers, temporal validation и локальный model card реализованы для proxy-задачи; отчёт по подтверждённым отказам невозможен без целевых лейблов. |
| 58 | Руководство пользователя и администратора | не реализовано | Нельзя составить достоверное руководство для несуществующих функций и контура идентичности. |
| 59 | Презентация в PPTX/PDF | не реализовано | Артефакт не создавался. |
| 60 | Демонстрационный прототип всех сценариев | не реализовано | Искусственная демонстрация запрещена пользовательским требованием; реальные сценарии требуют недостающих источников и моделей. |
| 61 | Ссылки поставки и питч-сессия | не реализовано | Не предоставлены и не формировались. |
| 62 | Проверки качества кода и ИБ | готово для текущего дерева | Единый локальный gate включает lint/typecheck/build, Vitest/pytest, Ruff, Bandit, Semgrep, TruffleHog, npm/pip audit, coverage и Git perimeter; результаты зафиксированы в `SECURITY_AUDIT.md`. |

## Вывод

Текущая работа закрывает безопасный переход от mock-данных к локальному
снимку, защищённому proxy-ML-контуру и fail-closed доставке прогноза в UI. Она
не изображает готовность тех функций, для которых нет источников или
подтверждённых лейблов. Полное соответствие ТЗ пока отсутствует: для него
владелец продукта должен предоставить контракты остальных источников,
разрешить целевой deployment-контур и принять эксплуатационные регламенты.

Процент готовности намеренно не вычисляется: ТЗ не задаёт веса требований, а
арифметическое усреднение статусов создало бы недостоверную оценку. Матрица
выше сохраняет проверяемое состояние каждого требования отдельно.
