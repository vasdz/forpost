[project]
name = "forpost-prediction-core"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "forpost-domain"
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/forpost_prediction_core"]